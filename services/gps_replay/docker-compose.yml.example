version: '3.1'
services:
  gps_replay:
    build:
      context: ../gps_replay
    environment:
    volumes:
#      - ../gps_replay/test_points.log:/opt/test_points.log
      - ../gps_replay/dbus:/var/run/dbus:rw

